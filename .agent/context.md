# Project Memory

- 2026-04-23: Project started from a near-empty repository with only `SPEC.md`. The implementation should treat `SPEC.md` as the source of truth and keep reusable decisions here.
- 2026-04-23: User selected `Vite + Vue 3` for the frontend and `Vitest` for testing.
- 2026-04-23: Implemented the initial product as a standalone Vue SPA with `Vue Router`, `Pinia`, `IndexedDB`, `satellite.js`, `Three.js`, and a custom service worker rather than reusing an existing web stack.
- 2026-04-23: `vue-tsc -b` emitted `.js/.d.ts` files into the source tree in this setup; use `vue-tsc --noEmit && vite build` instead.
- 2026-04-23: The app is structured around a mock-first gateway that can transparently upgrade to `/api/*` edge endpoints later without changing the UI/store layer.
- 2026-04-23: Remaining scope gaps against the full SPEC are mainly pass calendar richness, full event scheduling UX, broader edge integration, and stronger bundle splitting around the 3D briefing view.
- 2026-04-23: Added v1-complete local UX for pass calendar/list toggle, scheduled events, simulation-time orbit control, fleet member displayName/tags overrides, conjunction filtering, decay/fleet intersection highlighting, and PWA update/install guidance.
- 2026-04-23: 3D globe code is now lazy-loaded into a separate chunk. The chunk is still large because of `three`, but the main briefing route no longer pays that cost upfront.
- 2026-04-23: `FleetStore` now includes event deletion, and the UI depends on that contract for the scheduled event timeline.
- 2026-04-24: Blank/empty dev screen was caused by stale PWA service-worker runtime caching plus IndexedDB/profile timing. In dev, unregister service workers and delete `orbit-lab-*` caches; `public/sw.js` self-unregisters on local hosts.
- 2026-04-24: `IndexedDBFleetStore` uses DB v2 safe store creation and falls back to volatile `InMemoryFleetStore` after a short timeout so bootstrapping cannot stop before seed fleet/events render.
- 2026-04-24: User requested Chrome testing in a separate session. Prefer headless/isolated Chrome with `--user-data-dir`, `--incognito`, `--ignore-certificate-errors`, `--test-type`, and host resolver rules; avoid GUI Chrome because it crashed in `HIServices` during launch.
- 2026-04-24: User rejected low-quality Orbit View. Default desktop render mode is now 2D because the operational ground-track is more readable; 3D remains available via toggle. Orbit UI should be treated as product surface, not debug SVG/canvas.
- 2026-04-24: User benchmarked against 4CE crisis dashboard and World Monitor. Briefing should be map-first situational awareness, with a war-room hero, live intel queue, active layer list, and secondary cards below; avoid generic card-grid-first dashboards.
- 2026-04-24: 2D Orbit View must be interactive, not a fixed viewport. It now uses dynamic SVG viewBox state for wheel zoom, drag pan, Shift+wheel pan, zoom buttons, and reset.
- 2026-04-24: User requested `npx getdesign@latest add bmw`; `DESIGN.md` was installed and UI should follow BMW-inspired sharp, angular, blue-accent styling. Orbit 2D now uses OpenStreetMap raster tiles with attribution; service worker must not cache cross-origin OSM tiles.
- 2026-04-24: BMW styling was rejected as insufficient; user switched to `npx getdesign@latest add playstation`. Active design direction is now PlayStation-inspired: console-black hero/map surfaces, paper-white panels, PlayStation Blue `#0070cc`, Cyan `#1eaedb` only for interaction, pill CTAs, and rounded 12-24px feature frames. Do not reintroduce square-corner BMW treatment.
- 2026-04-24: User decided install UX is not needed. Removed app install prompts/buttons and the manifest link; keep service worker update/offline mechanics only. Orbit 2D now labels live mode as SGP4 live 1x and draws smoother Catmull-Rom SVG paths from denser SGP4 samples instead of coarse straight polylines.
- 2026-04-24: Live orbit at true 1x is too slow to perceive on the map. Default Briefing live visualization is now 60x with explicit 1x/60x/300x controls and second-level map clock; rate changes preserve the current displayed orbit time.
- 2026-04-24: Live orbit controls failed because simulation-time changes triggered `usePassPredictions` to `postMessage` Vue reactive proxies into a Worker, causing `DataCloneError` and aborting component updates. Worker payloads must be normalized into plain cloneable objects before posting, and multi-word component events should use explicit kebab-case names at the component boundary.
- 2026-04-24: User corrected Live orbit default speed: it must default to real-time 1x, not 60x. Keep speed controls available without layout shift by preserving the rate-control row in simulation mode as disabled, and keep the datetime input populated in live mode.
- 2026-04-24: 2D/3D view switching should use a stable segmented control, not a single text-changing toggle button. Keep control widths/heights stable across render-mode changes and avoid compressing orbit time-jump buttons; use 5x2 desktop/tablet layout and horizontal chips on very small screens.
- 2026-04-24: Domain research confirmed Fleet Health/FMC/PMC/NMC is not publicly verifiable satellite truth; it is an internal/user readiness concept. CelesTrak SATCAT active/operational status is not equivalent to mission-capable, and UI should avoid exposing implementation labels like `Briefing P1` as if they are domain terms.
- 2026-04-24: Readiness UI now avoids false 0% mission-capable claims when no operator status exists. It shows recorded/total coverage, `—` for unmeasured FMC/PMC/NMC rates, and labels the card as USER-entered operator readiness.
- 2026-04-24: Cloudflare Worker deployment path added. `wrangler.toml` serves Vite `dist` via Worker Assets and `worker/index.ts` handles `/api/*` with real CelesTrak GP + NOAA SWPC summary normalization, fallback SOCRATES/decay/default ground stations, Worker-side API caching, and browser `no-cache` API responses. Actual deploy is blocked until Wrangler auth is fixed.
- 2026-04-24: User emphasized that every button/component needs a reason to exist. Removed the always-on topbar "실시간 브리핑" chip, kept topbar status only for offline/update cases, converted non-action Map Layers buttons into status rows, and made list/calendar toggles show selected state. 3D Globe now consumes the same displayed orbit time as 2D so Live speed/time controls affect both render modes.
- 2026-04-24: User confirmed the Worker deployment was completed externally before this UI cleanup; continue validating deployment readiness with `npm run worker:dry-run` after front-end changes.
- 2026-04-24: 3D Globe surface polygons are still intentionally offline/procedural, but now use denser hand-authored coastline polygons, smoothed canvas paths, land-only terrain noise, and city-light clusters. If higher geospatial fidelity is needed later, replace this with a real low-res Natural Earth/TopoJSON asset rather than continuing to hand-author coordinates.
- 2026-04-24: User rejected hand-authored globe coastlines because Korea/Japan were missing. 3D Globe now uses a local downsampled NASA Blue Marble topography raster at `public/textures/earth-blue-marble-topo.jpg`; keep map textures local rather than runtime remote URLs. Added public/default ground station set from NASA NSN/DSN, ESA/SSC/ASF/KSAT/SANSA references and bootstrap now merges missing defaults into existing workspaces.
- 2026-04-24: Deployed current Worker build successfully to `https://celestrak-orbit-lab-pro.weston0713.workers.dev` with Version ID `92a5a5f2-9d63-4d68-81ea-9d739e764134`; post-deploy curl returned HTTP 200 and `/api/ground-stations` returned 14 stations.
- 2026-04-24: Catalog Browser previously appeared limited because Worker only fetched `stations`, `weather`, and `gps-ops`, then truncated to 120 entries. Worker now includes CelesTrak `active` GP TLE by default, enriches from SATCAT active JSON when available, deduplicates by NORAD catalog number, and serves up to 20,000 entries. Catalog UI must page/render a visible subset rather than v-for rendering the full active catalog.
- 2026-04-24: Deployed expanded catalog build to `https://celestrak-orbit-lab-pro.weston0713.workers.dev` with Version ID `b4d7b2c9-fbe5-4591-8227-9b454af25567`; post-deploy `/api/celestrak/catalog?limit=20000` returned HTTP 200, 8,744,966 bytes, and 15,161 catalog entries.
- 2026-04-24: UX decision from catalog/fleet review: treat Catalog as an OSINT source index and Fleet/Watchlist as the user-selected tracked set that drives Briefing. Briefing should not imply SATCAT objects are “selected” globally; use labels like “Tracked Objects” / “Selected Fleet”, provide remove/manage/add-from-catalog links from Briefing, and make Catalog row actions say “Track in Briefing” with added-state feedback.
- 2026-04-24: Implemented catalog-to-briefing UX link: Catalog rows/cards now show tracked-state feedback for the selected fleet, Briefing has a `Tracked Objects` selected-fleet card with remove/manage/add actions, and `OriginBadge`/panel action CSS now constrains long timestamps with nowrap + ellipsis to prevent header breakage.
- 2026-04-24: User clarified OriginBadge timestamps should reserve a stable timestamp-sized column rather than aggressively truncating based on viewport. `origin-badge small` now uses a fixed 9.75rem inline size so Korean date/time labels remain readable and panel actions wrap as needed.
- 2026-04-24: Further OriginBadge correction: one-line source+timestamp pill looked awkward when timestamp width was reserved. OriginBadge now uses a two-row meta chip (`source` row, fixed-width timestamp row) so the provenance label stays compact and the timestamp remains readable without stretching panel actions horizontally.
- 2026-04-24: OriginBadge with no timestamp should render as a compact one-row source chip, not the two-row timestamp chip. Cards with optional timestamps should not render a source-only action badge when data is absent. Added per-fleet hidden tracked refs in preferences so Briefing can hide/show objects from map/pass display without removing them from the selected fleet.
- 2026-04-24: Live `/api/celestrak/catalog?limit=20000` returned 502 because one CelesTrak group fetch (`stations`) aborted and Worker used `Promise.all`, causing one failed upstream group to fail the whole catalog. Worker catalog now isolates group failures with `Promise.allSettled`, raises CelesTrak fetch timeout to 20s, bumps cache version to `v3`, and was redeployed as Version ID `28e0dc5e-0190-4953-89cd-caf668c5fd81`; post-deploy full catalog returned HTTP 200 with 15,161 entries.
- 2026-04-24: Catalog tracking action is now a toggle. `Track` adds the object to the selected fleet and `Tracking` removes it from the selected fleet instead of being disabled, matching the user's expectation that the same Catalog control can undo the addition.
- 2026-04-24: 3D globe did compute SGP4 altitude but visual radius compressed LEO objects into the same minimum band and MEO/GEO into the same cap. `radiusForAltitude` now uses a log-based scale with a wider 0.08-1.18 altitude offset so LEO/MEO/GEO bands and LEO internal altitude differences remain visible without using physically exact scale.
- 2026-04-24: Mobile UX correction: bottom nav should be opaque, compact, and auto-hide on downward scroll; Briefing map controls should not consume map space by default. Added collapsed mobile map bar plus CSS fullscreen/focus mode. Focus mode needs the Briefing root elevated above the sticky topbar because `war-room__main` creates a lower stacking context.
- 2026-04-24: Mobile map controls refinement: 2D/3D render-mode switching must remain outside the collapsible simulation-control panel, the map focus CTA label should be explicit (`전체화면`), and both 2D SVG and 3D WebGL maps should support two-finger pinch zoom in addition to wheel zoom.
- 2026-04-24: 3D Orbit Globe should avoid duplicate LIVE/SIM indicators. Removed the left header mode pill and kept the right clock/status block as the single source of orbit mode + timestamp. Deployed to Cloudflare Worker Version ID `a2bf2e93-31d7-4f29-8d4b-10ad3b854a3c`.
- 2026-04-24: Fleets page needed product-level layout rather than raw SPEC form panels. Added a dark fleet command summary, replaced `F-MUST-*` subtitles with user-facing labels, split Directory/Status/Members/Import/Anomaly into responsive grid panels, and converted Fleet Members rows into mobile-safe cards.
- 2026-04-24: Added link preview/favicon assets for the custom domain. `index.html` now uses `https://celtrak.blue-rabbit.kr/` canonical/OG URLs, `/icons/favicon.svg`, `/icons/apple-touch-icon.png`, and `/og-image.png`. PNG assets are generated from SVG sources with `rsvg-convert`; direct Chrome SVG screenshots can leave white/cropped margins.
- 2026-04-25: Loading UX should be explicit during initial data sync. `AppShell` now blocks route content with a global `LoadingState` until first sync completes, the topbar shows the current loading phase, and Catalog has an inline loading state when catalog data is still empty.
- 2026-04-25: Added GitHub Actions deployment workflow for `main` pushes. CI runs `npm ci`, `npm test`, then `npm run deploy` with `CLOUDFLARE_ACCOUNT_ID` and `CLOUDFLARE_API_TOKEN` secrets.
- 2026-04-25: Catalog cold-start optimization: bootstrap loads only selected-fleet CATNR entries, Catalog route lazy-loads the full active index, and Worker catalog cache uses canonical keys, 2h refresh/2.5h retention, and scheduled prewarm because CelesTrak GP/SATCAT does not expose expiry headers.
- 2026-04-25: V1 orbit contact/focus scope keeps ground-station selection tied to existing `enabled`, treats `elevationMaskDeg` as user override, uses SOCRATES only for CDM severity, and adds session-only map focus plus 2D terminator/contact-link rendering.
- 2026-04-25: Orbit focus UX follow-up: 2D night mask now uses smooth terminator polygons with `evenodd`, 2D map clicks use pointer hit-testing so satellites can be selected after pan/zoom handling, 3D picking attaches focus only to marker/picker meshes and includes an auto-rotation pause button, and Briefing exposes enabled ground-station selection next to the map.
- 2026-04-25: User found the `evenodd` 2D night mask disappeared in practice. Replace subtraction-style daylight holes with direct night-region polygons using analytical daylight intervals, remove `fill-rule`, and keep the night overlay visibly opaque. Briefing should prioritize interactive Focus Inspector + Ground Stations; passive Live Intel Queue and Map Layers belong below the map.
- 2026-04-25: Localhost Catalog showing only 4 rows means Vite served SPA HTML for `/api/*`, so gateway fell back to bundled mock catalog. Add a Vite dev proxy to the deployed Worker API for local development; verify localhost `/api/celestrak/catalog` returns JSON before debugging Catalog UI.
- 2026-04-25: 2D map should wrap horizontally rather than clamp at the dateline. Keep y clamped, repeat OSM/world overlays/markers by world copy, and draw contact links to the nearest wrapped satellite x so 2D east/west direction matches 3D. `보기`/map focus should center the target and pause 3D auto-rotation.
- 2026-04-25: 3D ground-station colors must carry meaning. White is focused, cyan means the station currently has an active contact link, dim blue is enabled but idle. Avoid arbitrary alternating colors. Pass prediction worker must run immediately on Briefing mount and should not globally truncate too early, otherwise visible contacts can show LOS/AOS as `계산 중` even though look-angle calculation is instant.
- 2026-04-25: 3D focus must not use a raw quaternion that changes roll. Keep a canonical `globeRotation` state, lock z/roll to the initial axis, solve focus with x/y only, and have drag/auto-rotate/reset all read/write that same state so interaction after focus starts from the focused pose.
- 2026-04-25: Catalog `Track` appeared to work but disappeared after refresh because reactive Vue fleet objects were passed into IndexedDB, causing `DataCloneError`; the store then permanently fell back to in-memory state. Clone persistence payloads before `put`, avoid permanent fallback on transient timeouts, and persist `selectedFleetId` in preferences.
- 2026-04-25: Focus centering should feel continuous, not like a jump. 2D map focus now animates center/zoom with ease-out and cancels on user navigation; 3D globe focus animates rotation with the same canonical rotation state and cancels on drag/auto-rotate/reset.
- 2026-04-25: After smooth 3D focus, the separate globe `축 초기화` button is no longer needed. Keep auto-rotation pause/resume, but avoid extra orientation controls that duplicate focus behavior.
- 2026-04-25: 2D map flicker with many tracked satellites is caused by high-volume SVG node patching. For >20 plotted satellites, render dynamic tracks/links/stations into one RAF-batched canvas layer and keep SVG for base map/night mask; keep SVG mode for small fleets to preserve simple DOM interaction.
- 2026-04-25: UI screenshot pass found oversized pill buttons and mobile bottom-nav crowding. Keep global buttons at 40px min height, avoid hover scale transforms, compact dense panel/table buttons, use one-row desktop time-jump controls, and hide the 2D map clock on mobile so map nav does not collide with bottom tabs.

- 2026-04-25: Screenshot-based UI review should use full-page captures, not viewport-only. Full-page Briefing review found Tracking Scope/Ground Station lists can clip inside the war-room; keep expanded side lists shorter and internally scrollable.

- 2026-04-25: 2D map satellite labels must stay screen-sized while zooming; scale label bounds/rects inversely to map zoom and keep label hitboxes in sync. Pass prediction runs in a Web Worker; for many tracked objects, prioritize focused satellite/station predictions and preserve first pass per pair so AOS/LOS does not fall back to `계산 중` from global truncation.

- 2026-04-25: 2D night mask should indicate darkness without hiding OSM map detail; keep the overlay around rgba alpha 0.48 rather than the previous very dark 0.68.

- 2026-04-25: Satellite colors are semantic now: focused white, risk red, active contact cyan, normal tracked blue. Do not reintroduce order-based rotating satellite palettes because users read colors as state.

- 2026-04-25: CDM V1.5 direction is SOCRATES Plus ingest, not in-app all-vs-all. Worker parses CelesTrak raw CSV (`/SOCRATES/sort-minRange.csv`) with limit/catnr filtering; Briefing exposes 전체/Tracked/Focused CDM scopes.

- 2026-04-26: Ground-station elevation is strictly local `GroundStation.elevationMaskDeg` user override/default data, not externally queried hardware metadata. Contact countdowns should avoid `계산 중` by preserving open-ended pass windows through the prediction horizon, using 1-minute focused priority passes, and falling back to cached focused AOS/LOS threshold scans. Ground Stations now has its own top-level route with all-station controls and worker-backed current visible-satellite scans.

- 2026-04-26: Ground-station elevation masks now carry provenance metadata (`verified`/`inferred`/`default`/`user`). Keep numeric masks user-overridable, show source confidence in Briefing/Ground Stations/More, and backfill local default source metadata even when the deployed Worker `/api/ground-stations` response has not yet been updated.
- 2026-04-26: Contact Windows countdowns that show whole-minute values like `LOS까지 00:03:00` usually come from `buildLiveContactLinks` fallback threshold scans, not display formatting. The fallback scans LOS in 60s steps, AOS in 120s steps, and caches by minute when no matching active/next pass prediction exists; el/az remains live because look angles are recomputed at the current displayed orbit time.
- 2026-04-26: Pass predictions now record the station `elevationMaskDeg` used for AOS/LOS, and live contact links ignore predictions whose mask no longer matches the current station setting. Fallback countdowns are flagged as estimated so Briefing labels them with `약`; pass predictions are cleared when a new worker request starts so station-mask edits do not leave stale windows in the UI while recomputing.
- 2026-04-26: Keep broad pass prediction in a worker, but refine only focused Contact Windows with a separate client worker. The precision worker scans the focused link in 15s brackets, binary-searches to ~1s, returns an event timestamp, and Briefing keeps the previous countdown visible with `(재계산 중)` while recomputing.
- 2026-04-26: User clarified satellite name rect omission is acceptable in dense 2D canvas mode, but 3D rendered satellites should all expose labels. More was merged into Ground Stations to keep mobile bottom tabs to one row; `/more` now redirects to `/stations`.

- 2026-04-26: Focus Inspector entity names should act as lightweight focus chips. Hover/focus on chips previews the target on 2D/3D maps with a cyan preview state, while the committed focus remains white so preview and focus are visually distinct.

- 2026-04-26: Map view colors should mirror Focus Inspector semantics. Use critical red `#c81b3a`, warn amber `#f5c84b`, active-contact cyan `#1eaedb`, tracked blue `#53b1ff`, focused white, and dim gray for idle ground stations; avoid reusing warn/critical colors for contact-window AOS/LOS.

- 2026-04-26: Desktop `AppShell` sidebar can collapse to an 86px rail. Keep nav usable through marker buttons with hover/focus labels, persist the state in localStorage, and avoid overlapping the collapsed rail toggle with the brand mark.

- 2026-04-26: Orbit controls should treat returning to Live as a reset to real-time 1x. CDM relative labels in Briefing should be based on displayed ORBIT TIME and prefer hour/minute countdowns instead of day-level labels. When a satellite is focused, both 2D and 3D map views should keep following that moving target.

- 2026-04-26: CDM Focus Inspector chips can represent untracked catalog objects; clicking an untracked CDM chip should add that catalog object to the selected fleet before focusing it. Keep CDM chip rows single-line with truncation, and keep collapsed sidebar controls fully inside the rail with high-z hover labels.

- 2026-04-26: Orbit Time jumps and manually applied times should enter a flowing simulation playback state, not a frozen snapshot. The playback speed applies to both live and simulation time, the UI label is `Speed`, and the control is a 1x-300x slider rather than fixed preset buttons.

- 2026-04-26: Rebase with `main` combined flowing simulation playback with focused Contact Window precision. Precision worker request keys should use the simulation anchor (`store.simulationTimeIso`) instead of the displayed ticking orbit time, otherwise flowing simulation mode can re-request precision every orbit-clock tick.

- 2026-04-26: Focused satellites should stop auto-following after user map/globe navigation while retaining Focus Inspector selection, matching ground-station focus behavior. Speed direction is a signed playback rate; keep the UI as a single slider plus a tape-control style direction toggle (`>>` forward / `<<` rewind). Individual CATNR Worker lookup should fetch SATCAT and TLE concurrently and tolerate one-source failure so `/api/celestrak/catalog?catnr=...` does not hang behind a slow upstream branch.

- 2026-04-26: CDM untracked-object add needs explicit async feedback. Show a small Focus Inspector toast immediately on request, replace it with success/error, and keep retry on error. For Worker CATNR lookup, do not wait on slow SATCAT enrichment once TLE is available; use shorter CATNR-specific upstream timeouts and bump cache version when changing this behavior.
- 2026-04-26: CDM add feedback should be a viewport-level top-right toast layer, not embedded inside Focus Inspector. Users can miss inline Inspector feedback while scanning the map; use state-colored Grafana-like toasts for requested/success/failed outcomes.
- 2026-04-26: CDM CATNR add failures can be caused by CelesTrak connection throttling, not necessarily missing objects. Keep Worker CATNR lookup TLE-first to avoid opening simultaneous TLE+SATCAT connections, include response-body reads in upstream timeouts, return empty rows for true no-data, and keep obvious `UNKNOWN` CDM objects non-clickable.
- 2026-04-26 correction: CDM `UNKNOWN` objects should still be visible and represented as chips because they may be risk-relevant even if not publicly resolvable. Clicking them should show a non-retryable "cannot add to tracking" toast instead of hiding the chip or sending catalog lookup.
- 2026-04-26: NOAA 19/SUOMI hover mismatch root cause was stale seed/mock mapping: NORAD `37849` is SUOMI NPP, while NOAA 19 is `33591`. Bootstrap now repairs legacy local fleet/event refs from `37849 + NOAA 19` to `33591`, and Briefing map labels honor selected-fleet display names so aliases like GPS BIIR-2/NAVSTAR 43 stay visually consistent.
- 2026-04-26: Orbit playback pause should be represented as `livePlaybackRate = 0` while preserving the last non-zero signed speed separately for resume. Tracked Objects action buttons need sidebar-specific compact CSS with enough card height; generic `panel-card__action-link` sizing can override earlier compact rules if specificity is too low.

- 2026-04-26: iPad 2D map slowness likely comes from small tracked sets staying in SVG mode while still recomputing TLE parsing, full trails, night mask paths, and deep redraw watchers every 250ms. Touch/tablet 2D maps now switch dynamic layers to canvas from medium track counts, cache satrec/trail segments, quantize trail/night-mask refresh, and keep SVG mode mainly for small non-touch cases.

- 2026-04-26: iPhone/iPad 2D map slowness can persist even after dynamic objects move to canvas because Safari still composites SVG OSM tiles, CSS filters, blend overlays, backdrop filters, and high-DPR canvas redraws. Touch 2D maps now use a rendering budget: lower OSM tile zoom, no tile/backdrop filters, lower canvas DPR, coarser trails, and RAF-batched drag center updates.

- 2026-04-26: When stacking feature branches before develop merges, do not assume prior PR UI exists on new branches. Tracked Objects bulk visibility controls must stay in the Tracking Scope tab, with a two-row toolbar (`모두 보이기`/`모두 숨기기`, then Catalog/Fleet links) so sidebar buttons do not crowd object cards.

- 2026-04-26 correction: Do not globally coarsen 2D trail samples just because the device is touch-capable. If visible tracked objects drop below the touch canvas threshold, the SVG renderer needs the original 2-minute trail samples; otherwise ground tracks look visibly wrong. Keep coarser samples limited to dense/canvas/data-saver paths.
