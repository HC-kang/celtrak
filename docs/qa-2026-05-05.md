# QA Report - 2026-05-05

## Scope

This QA pass covers the current `develop` branch changes for:

- Tracking Scope / Tracked Objects card layout stabilization.
- SWPC Space Weather fallback/cache handling.
- PWA install CTA visibility from the current branch.
- Desktop and mobile navigation, route surfaces, and safe UI tabs/menus.

Local target:

- `http://127.0.0.1:5182`

Viewports:

- Desktop: `1365 x 900`
- Mobile: `390 x 844`, device scale factor `2`

Routes checked:

- `/briefing`
- `/catalog`
- `/fleets`
- `/stations`
- `/weather`

## Automated Checks

Commands run:

- `npm run test` - passed, 47 tests.
- `npm run build` - passed.
- `npm run worker:dry-run` - passed.

Browser automation checks:

- Route loads completed without the global loading screen remaining visible.
- No horizontal page overflow detected.
- No repeated-card sibling overlap detected for common list/card containers.
- No `FALLBACK` / `stale-safe fallback` text detected on current Space Weather screens.
- Mobile drawer opened and exposed all 5 primary navigation links.
- Briefing safe controls were exercised:
  - Priority Watch expand.
  - Tracking Scope: `Ground Stations` / `Tracked Objects`.
  - CDM scope: `전체` / `Tracked`.
  - Orbit view mode: `2D Track` / `3D Globe`.
- Ground Stations safe controls were exercised:
  - `리스트` / `캘린더`.

Screenshot artifacts were captured under:

- `/private/tmp/celtrak-ui-qa/`

## Route Results

| Route | Desktop | Mobile | Notes |
| --- | --- | --- | --- |
| Briefing | Pass | Pass | Tracking Scope object cards no longer overlap action chips into the next card. Priority Watch and CDM tabs work. |
| Catalog | Pass | Pass | Search/filter controls render without horizontal overflow. Tracking toggle was not clicked to avoid mutating fleet state. |
| Fleets | Pass | Pass | Directory, status, members, import, and anomaly panels render without sibling overlap. Destructive member/fleet actions were not clicked. |
| Ground Stations | Pass | Pass | Main controls render without horizontal overflow. `리스트` / `캘린더` view switching works. Bulk enable/disable was not clicked. |
| Space Weather | Pass | Pass | Current SWPC data renders; fallback copy is not present. |

## Recurring UI Breakage Notes

The repeated failure pattern has been card/list layouts that visually depend on a fixed or underestimated height while the content row contains wrapped chips/buttons.

Observed root causes:

- Dense action chips were allowed to overflow visible card bounds.
- Grid rows did not always expand around wrapped action rows.
- Generic button sizing rules overrode compact sidebar/card-specific rules.
- Mobile cannot rely on hover states to distinguish chips from buttons, so visual affordance needs to be explicit.

Required QA checks for future UI changes:

- Test both desktop and mobile widths before shipping any panel/card/list change.
- For repeated cards, verify adjacent sibling rectangles do not overlap.
- Check that source chips and action buttons remain inside the card after labels wrap.
- Verify the same component after state changes, not only on initial render.
- Avoid `overflow: visible` on dense cards unless the parent layout explicitly reserves space.
- Keep action rows in normal layout flow, preferably flex column/row wrapping with bounded labels.
- Treat Service Worker and Worker cache versions as part of UI QA when stale fallback data affects what the user sees.

## Known Non-Destructive QA Exclusions

The following controls were intentionally not clicked during automated QA because they mutate user workspace data or browser/device permissions:

- Fleet member remove/delete actions.
- Ground station bulk enable/disable.
- Catalog tracking toggle.
- Current location request.
- Workspace import/replace.
- Event/anomaly/status create/close actions.

These require a separate seeded-state or disposable-profile QA pass if full mutation coverage is needed.
